{{- if .Values.allInOne.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "pomerium.allInOne.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ template "pomerium.allInOne.name" . }}
    helm.sh/chart: {{ template "pomerium.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- if .Values.service.labels }}
{{ toYaml .Values.service.labels | indent 4 }}
{{- end }}
{{- if or .Values.allInOne.service.annotations .Values.service.annotations }}
  annotations:
  {{- if .Values.allInOne.service.annotations }}
    {{- range $key, $value := .Values.allInOne.service.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- else if .Values.service.annotations }}
    {{- range $key, $value := .Values.service.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
{{- end }}
spec:
  type: {{ .Values.allInOne.service.type }}
  ports:
    - port: {{ template "pomerium.service.externalPort" . }}
      targetPort: {{ template "pomerium.httpTrafficPort.name" . }}
      protocol: TCP
      name: {{ template "pomerium.httpTrafficPort.name" . }}
{{- if and .Values.allInOne.service.nodePort (eq .Values.allInOne.service.type "NodePort" )}}
      nodePort: {{ .Values.allInOne.service.nodePort }}
{{- end }}
    - name: metrics
      port: {{ .Values.metrics.port }}
      protocol: TCP
      targetPort: metrics
  selector:
    app.kubernetes.io/name: {{ template "pomerium.allInOne.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
